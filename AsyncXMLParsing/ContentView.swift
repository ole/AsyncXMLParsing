import SwiftUI

struct ContentView: View {
  var body: some View {
    Text("Run the unit tests to see the code in action.")
      .padding()
  }
}

struct ContentView_Previews: PreviewProvider {
  static var previews: some View {
    ContentView()
  }
}
